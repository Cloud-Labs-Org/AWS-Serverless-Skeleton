// const environment = process.env;

export class Config {
  // public static REGION = environment.REGION as string;
  // public static DYNAMODB_DEVICE_DATA_TABLE = environment.DYNAMODB_TABLE_NAME as string;
  // public static CLIENT_POOL_ID = environment.CLIENT_POOL_ID as string;
  // public static DYNAMODB_TABLE_NAME = environment.DYNAMODB_TABLE_NAME as string;
}
